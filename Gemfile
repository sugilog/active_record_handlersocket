source "http://rubygems.org"

# Specify your gem's dependencies in active_record_handlersocket.gemspec
gemspec

# gem 'ruby-debug', '0.10.0' # if need

if RUBY_VERSION >= "1.9.3"
  gem 'guard',       '~> 2.6.1'
  gem 'guard-rspec', '~> 4.2.10'
  gem 'coveralls', :require => false
end
